{
  "$schema": "../node_modules/@angular-devkit/schematics/collection-schema.json",
  "schematics": {
    "ng-add": {
      "description":
        "Adds recommended extensions and configuration to debug an application generated by the Angular CLI.",
      "factory": "./install/index#install"
    }
  }
}
